git init
git add .
git commit -m "Primer commit: Mi proyecto de Peluquerías y Perfumerías"

git remote add origin https://github.com/encoder1024/PerPel.git
git branch -M main (opcional, dado que ya configuré el git localmente para que la branch principal se llamé main siempre)
git push -u origin main